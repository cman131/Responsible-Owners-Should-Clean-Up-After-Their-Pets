# Overrides

This folder contains project-specific exceptions to the standard skills. If you need to override a standard, create a file here with the same filename as the skill (e.g., `architecture.md`). See `.ai/instructions.md` for details.
